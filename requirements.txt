# API Authentication Service Dependencies

# Core Dependencies
urllib3>=2.0.0           # HTTP library with thread-safe connection pooling
python-dotenv>=1.0.0         # Variables de entorno

# PDF Processing & OCR
pymupdf>=1.23.0          # PyMuPDF (fitz) - Extracción PDF robusta y multiplataforma
paddleocr>=2.7.0         # OCR robusto basado en PaddlePaddle (sin dependencias del sistema)
paddlepaddle>=2.5.0      # Framework de deep learning de PaddleOCR
Pillow>=10.0.0           # Python Imaging Library (PIL Fork)
numpy>=1.24.0            # Requerido por PaddleOCR y procesamiento de imágenes

# ===== Logging =====
structlog>=24.1.0           # Logging estructurado
python-json-logger>=2.0.7    # JSON logging

# ===== HTTP Client =====
httpx[http2]>=0.26.0                # HTTP client async con HTTP/2
requests>=2.31.0

# ===== Retry & Resilience =====
tenacity>=8.2.3              # Retry logic robusto


# GUI
tkinter-tooltip>=2.1.0

# Consola con colores (Windows)
colorama>=0.4.6

python-dateutil>=2.9

